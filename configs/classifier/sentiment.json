{
  "dataset_name": "PersonalComment",
  "model_name_or_path": "hfl/chinese-roberta-wwm-ext",
  "output_dir": "outputs/classifier_sentiment/model",
  "train_file": "data/train",
  "validation_file": "data/valid",
  "test_file": "data/test",
  "per_device_train_batch_size": 64,
  "per_device_eval_batch_size": 64,
  "learning_rate": 1e-5,
  "weight_decay": 0.01,
  "max_steps": 200000,
  "logging_steps": 50,
  "eval_steps": 10000,
  "save_steps": 10000,
  "dataloader_num_workers": 4,
  "use_fast_tokenizer": false,
  "num_labels": 2,
  "max_persona_len": 64,
  "max_article_len": 510,
  "max_comment_len": 32,
  "max_seq_length": 510,
  "limit_length": 600000,
  "training_data_type": "PersonaComment"
}
