{
  "dataset_name": "PersonalComment",
  "model_name_or_path": "hfl/chinese-bert-wwm-ext",
  "output_dir": "outputs/classifier_age/model",
  "train_file": "data/train",
  "validation_file": "data/valid",
  "test_file": "data/test",
  "per_device_train_batch_size": 256,
  "per_device_eval_batch_size": 256,
  "learning_rate": 1e-5,
  "weight_decay": 0.01,
  "max_steps": 300000,
  "logging_steps": 50,
  "eval_steps": 500,
  "save_steps": 5000,
  "dataloader_num_workers": 4,
  "use_fast_tokenizer": false,
  "num_labels": 4,
  "max_persona_len": 64,
  "max_article_len": 510,
  "max_comment_len": 32,
  "max_seq_length": 510,
  "limit_length": 600000,
  "training_data_type": "PersonaComment"
}
